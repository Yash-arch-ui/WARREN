# Rust build artifacts
/target

# Local state that warren will create at runtime (identity keys, config)
# NOTE: identity material must NEVER be committed.
.warren/
*.key
*.pem

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store
